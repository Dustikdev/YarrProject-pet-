import UIKit

struct Images {
    
    struct SystemImages {
        static let plus = UIImage(systemName: "plus")
        static let photo = UIImage(systemName: "photo.fill")
        static let trash = UIImage(systemName: "trash")
        static let pencil = UIImage(systemName: "square.and.pencil")
        static let xmark = UIImage(systemName: "xmark")
    }
    
    struct AssetImages {
        static let defaultProjectIcon = UIImage(named: "default_image")
        static let cameraImage = UIImage(named: "capture_photo")
    }
}
